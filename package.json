{
  "name": "keyauth-selfhosted",
  "version": "1.0.0",
  "description": "Self-hosted KeyAuth 1.3 clone — unlimited free license generation",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "prisma generate",
    "vercel-build": "prisma generate && prisma db push --skip-generate",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "start": "node server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "nanoid": "^3.3.11"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
